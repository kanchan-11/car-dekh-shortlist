services:
  backend:
    build: ./backend
    container_name: car-copilot-backend
    ports:
      - "8080:8080"
    restart: always

  frontend:
    build: ./frontend
    container_name: car-copilot-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always
